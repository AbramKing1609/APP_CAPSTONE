<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <!-- ============================================== -->
    <!-- 1. ENCABEZADO SUPERIOR VERDE/CIAN (FIJO) -->
    <!-- ============================================== -->
    <FrameLayout
        android:id="@+id/fixedTopHeader"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/header_top_background"
        android:paddingTop="10dp"
        android:paddingStart="20dp"
        android:paddingEnd="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|bottom"
            android:text="Dr_Pe"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="@font/montserrat_extrabold"
            android:textColor="#FFFFFF" />
    </FrameLayout>

    <!-- ============================================== -->
    <!-- 2. CONTENIDO PRINCIPAL DEL LOGIN -->
    <!-- ============================================== -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:background="@android:color/white"
        android:padding="24dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_alignParentTop="true"
                android:layout_marginTop="16dp">

                <!-- Título principal -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Dr-Pe"
                    android:textSize="60sp"
                    android:textStyle="bold"
                    android:gravity="center"
                    android:textColor="#000000"
                    android:layout_marginTop="16dp"
                    android:fontFamily="@font/montserrat_extrabold" />

                <!-- Logo -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:layout_marginBottom="32dp">

                    <ImageView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:src="@drawable/ic_app"
                        android:contentDescription="@string/app_name" />
                </LinearLayout>

                <!-- Botones login/registro con fuente consistente -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="75dp"
                    android:orientation="horizontal"
                    android:background="@drawable/boton_doble_unido"
                    android:clipToOutline="true"
                    android:elevation="4dp"
                    android:outlineProvider="background">

                    <Button
                        android:id="@+id/btnLogin"
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:text="Ingresar"
                        android:textColor="@android:color/white"
                        android:textSize="18sp"
                        android:textStyle="bold"
                        android:fontFamily="@font/montserrat_extrabold"
                        android:backgroundTint="#20CCA9"/>

                    <Button
                        android:id="@+id/btntvGoRegister"
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:text="Registrarme"
                        android:textColor="#555555"
                        android:textSize="18sp"
                        android:textStyle="bold"
                        android:fontFamily="@font/montserrat_extrabold"
                        android:backgroundTint="#EBEBEB"/>
                </LinearLayout>
            </LinearLayout>
        </RelativeLayout>
    </ScrollView>

    <!-- ============================================== -->
    <!-- 3. PIE DE PÁGINA INFERIOR VERDE/CIAN (FIJO) -->
    <!-- ============================================== -->
    <FrameLayout
        android:id="@+id/fixedBottomFooter"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:background="@drawable/footer_bottom_background">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Sistema Médico"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="@font/montserrat_extrabold"
            android:textColor="#FFFFFF" />
    </FrameLayout>
</LinearLayout>
